{
  "name": "portfolio_",
  "version": "1.0.0",
  "description": "Portfolio de Aaron - Développeur Fullstack",
  "scripts": {
    "sass:watch": "sass --watch src/sass:dist/css --style compressed --source-map",
    "sass:build": "sass src/sass:dist/css --style compressed --no-source-map",
    "start": "npm run sass:watch"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}